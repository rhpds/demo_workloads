[defaults]
host_key_checking = False
retry_files_enabled = False
stdout_callback = default
roles_path = ../../roles
interpreter_python = auto_silent

[inventory]
enable_plugins = troshka.cloud.troshka, ansible.builtin.auto, ansible.builtin.yaml, ansible.builtin.ini
